# ─── RESEND (primary email provider) ───
# Get your API key at https://resend.com/api-keys
# Paste it below (without quotes) and rename this file to .env.local
RESEND_API_KEY=re_HyV4gykj_KouFmA3GVsPzTjCtQSaVBpHr

# ─── FORM SUBMIT (fallback — no config needed) ───
# Set to "true" to use FormSubmit as fallback BEFORE Resend domain is verified.
# FormSubmit works instantly on deploy (no DNS setup).
# To verify hayertechnologies.tech for Resend inbox delivery:
#   1. Add DKIM/SPF records from Resend dashboard → Domains → hayertechnologies.tech
#   2. Wait for DNS propagation (5 min–24 hrs)
#   3. Click "Verify" in Resend dashboard
# FORM_SUBMIT_FALLBACK=true
